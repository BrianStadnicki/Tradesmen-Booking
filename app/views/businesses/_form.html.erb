<%= simple_form_for @business do |f| %>

  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :address %>
  <%= f.input :phone %>
  <% if current_user.admin? %>
    <%= f.association :owner %>
  <% end %>
  <%= f.button :submit %>

<% end %>
