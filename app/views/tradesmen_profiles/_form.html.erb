<%= simple_form_for @tradesmen_profile do |f| %>

  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :tradesmen_trades %>
  <% if current_user.admin? %>
    <%= f.association :owner %>
  <% end %>
  <%= f.button :submit %>

<% end %>
