<%= form_with(model: tradesmen_trade) do |form| %>
  <% if tradesmen_trade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tradesmen_trade.errors.count, "error") %> prohibited this tradesmen_trade from being saved:</h2>

      <ul>
        <% tradesmen_trade.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
